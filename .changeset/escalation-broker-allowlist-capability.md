---
"cartograph": patch
---

The Resolution Strategy modal no longer offers escalation it cannot run: a gap whose edge label isn't in the bounded T3 broker's allowlist (for example `IMPORTS`) now shows both strategy cards as unavailable with a plain-language reason, instead of failing with a raw internal error ("invalid bounded task: edge label ... is outside the broker allowlist") after you click through.
