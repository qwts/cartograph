{{!-- cargo-about template for the Rust section of THIRD-PARTY-NOTICES.md
      (#222). Raw (unescaped) mustache blocks emit Markdown, not HTML, so the
      license bodies are not entity-escaped. --}}
Cartograph bundles the following Rust crates. Each is used under the terms of
its respective license, reproduced below. This list is generated from the
dependency lockfile by `cargo about`; do not edit by hand.

{{#each overview}}
- {{{name}}} ({{count}} crate(s))
{{/each}}

{{#each licenses}}
## {{{name}}}

Used by:
{{#each used_by}}
- {{{crate.name}}} {{{crate.version}}}{{#if crate.repository}} — {{{crate.repository}}}{{/if}}
{{/each}}

```
{{{text}}}
```

{{/each}}
