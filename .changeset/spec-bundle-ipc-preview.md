---
"cartograph": patch
---

Cap the Spec Workbench's export_spec IPC payload per artifact (content and assertions), truncate the Workbench's artifact preview with a counted note instead of rendering the whole text, and fetch the complete artifact on demand for Copy artifact, Export bundle, and the Gaps & Drift surface.
