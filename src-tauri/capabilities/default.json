{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window (M0: core only).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
