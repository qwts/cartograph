---
"cartograph": patch
---

The Preflight surface now always shows the classification the register holds. Before, a preflight that overlapped a recovery of the same repo could leave the surface pending or empty, and a recovery re-run from Jobs → Retry never showed its reconciled report.
