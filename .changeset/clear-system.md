---
'cartograph': minor
---

Multi-ingest stacking is now stated, not silent: the Workspace lists every repo currently merged into the system (from the graph's own facts) and says that new ingests merge in. The destructive action speaks your language — "Clear system" — and its confirmation names exactly what is removed (every recovered fact, listed by repo) and what survives (job history and settings).
