---
"cartograph": patch
---

On Windows, Help links now open with every query parameter intact (the URL goes to the system URL handler instead of `explorer`), and neither the link launcher nor the `gh auth token` step flashes a console window. An OS with no known browser launcher logs the refusal instead of failing to build.
