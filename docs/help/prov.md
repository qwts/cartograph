# Provenance & Eval

Where the honesty is measured: tier distribution, per-extractor coverage
(files in scope vs files with facts), eval gates for the semantic tier, and
re-ingest history with whole-graph content hashes — determinism observable
as data. Counts here reconcile exactly with Workspace and the register.
